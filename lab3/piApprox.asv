function [tanDiff, sinDiff] = piApprox(termsInSum)

tanSeries = 0; %arctan(x) = series
sinSeries = 0;

n = termsInSum;
xForSin = 0.5;
xForTan = 1.0;

for i=0:n
    
    tanSeries = tanSeries + ( ((-1)^n) * (xForTan^(2*n + 1)) / ((2*n) + 1) ); %x = 1
    sinSeries = sinSeries + ( (1/(2^(2*n))) * nchoosek(2*n, n) * (xForSin^(2*n + 1)) / ((2*n) + 1) );
    
end

tanSeries
sinSeries